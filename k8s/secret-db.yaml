apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: rso
type: Opaque
stringData:
  # The real secret is applied by scripts/db/db-secret.sh from scripts/db/db.env.
  DATABASE_URL: "postgres://username%40server:REDACTED@server.postgres.database.azure.com:5432/courses?sslmode=require"
