apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: rso
type: Opaque
stringData:
  # For local/demo use. In Azure PG, replace with its URL.
  DATABASE_URL: "postgres://postgres:postgres@db:5432/courses"
